interface:
  display_name: "Deliver Code Change"
  short_description: "Implement and verify one bounded code change"
  default_prompt: "Use $deliver-code-change to implement and verify this bounded code change without expanding its approved scope."
